services:
  - type: web
    name: bookkeeping-backend
    runtime: node
    plan: free
    region: oregon
    rootDir: backend
    healthCheckPath: /health
    buildCommand: npm install --include=dev && npx prisma generate && npm run build && npx prisma db push --accept-data-loss --skip-generate && (npx tsx prisma/seed.ts || echo "seed skipped - database already populated")
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bookkeeping-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRY
        value: 7d
      - key: CORS_ORIGIN
        sync: false

  - type: web
    name: bookkeeping-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: bookkeeping-db
    databaseName: bookkeeping
    user: bookkeeping_user
    plan: free
    region: oregon
